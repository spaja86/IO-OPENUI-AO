{
  "name": "io-openui-ao",
  "version": "1.0.0",
  "description": "SPAJA Ekosistem — AI platforma, banka, menjačnica i IT kompanija na jednom mestu",
  "scripts": {
    "build": "npm run build:realtime && npm run build:chat",
    "build:realtime": "cd put-a-realtime-webrtc/web && npm install && npm run build",
    "build:chat": "cd put-b-chat-socketio/web && npm install && npm run build",
    "dev": "vite",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=18.x",
    "npm": ">=8.x"
  },
  "license": "MIT"
}
